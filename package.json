{
  "name": "spellbrawl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:party": "npm run dev --prefix party",
    "build": "node scripts/generate-sfx.mjs && tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "party:deploy": "npm run deploy --prefix party",
    "sfx": "node scripts/generate-sfx.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "partysocket": "^1.3.0"
  }
}
