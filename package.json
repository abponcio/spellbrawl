{
  "name": "spellbrawl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:party": "partykit dev",
    "build": "node scripts/generate-sfx.mjs && tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "party:deploy": "partykit deploy",
    "sfx": "node scripts/generate-sfx.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "partykit": "^0.0.115",
    "partysocket": "^1.3.0"
  }
}
